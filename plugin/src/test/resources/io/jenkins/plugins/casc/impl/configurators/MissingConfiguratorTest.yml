jenkins:
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: admin
          password: ${ENC,PKCS6,}
  authorizationStrategy:
    globalMatrix:
      permissions:
        - "Overall/Administer:authenticated"
